[package]
name = "hezhou-rhi"
version = "0.1.0"
edition = "2021"

[lib]
name = "hezhou_rhi"
path = "src/lib.rs"

[dependencies]
hezhou-core = { path = "../core" }
hezhou-geometry = { path = "../geometry" }
thiserror = "1.0"
parking_lot = "0.12"

[dev-dependencies]